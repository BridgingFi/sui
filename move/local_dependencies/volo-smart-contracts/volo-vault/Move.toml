[package]
name    = "volo_vault"
edition = "2024.beta"  # edition = "legacy" to use legacy (pre-2024) Move
published-at = "0xbbd1c5d3373ac836a1150329536654efe5ba7590d11de277590246d487d3257f"

[dependencies.Switchboard]
local = "../../switchboard-xyz-sui/on_demand"

[addresses]
volo_vault = "0xbbd1c5d3373ac836a1150329536654efe5ba7590d11de277590246d487d3257f"


# Named addresses will be accessible in Move as `@name`. They're also exported:
# for example, `std = "0x1"` is exported by the Standard Library.
# alice = "0xA11CE"


# override = true
# The dev-dependencies section allows overriding dependencies for `--test` and
# `--dev` modes. You can introduce test-only dependencies here.
# Local = { local = "../path/to/dev-build" }

[dev-addresses]
# The dev-addresses section allows overwriting named addresses for the `--test`
# and `--dev` modes.
# alice = "0xB0B"


# Sui and Suilend have conflicts in "MoveStdLib" and "Pyth"
# [dependencies.MoveStdlib]
# git      = "https://github.com/MystenLabs/sui.git"
# rev      = "mainnet"
# subdir   = "crates/sui-framework/packages/move-stdlib"
# override = true
